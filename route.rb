class Route

  attr_accessor :station.first, :station.last

  def initialize(station.first, station.last)
    @station.first = station.first
    @station.last = station.last
    @stations = stations
  end

  def delete
    @stations.delete(stations)
  end

  def add
    @stations.push(-2, stations)
  end

  def list
    @stations.each { |stations| puts "#{stations}"}
  end
end
